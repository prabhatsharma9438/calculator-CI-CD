name: CD for Calculator Project

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Java
        uses: actions/setup-java@v3
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Make Maven Wrapper Executable
        run: chmod +x mvnw

      - name: Build JAR
        run: ./mvnw clean package -DskipTests

      - name: Clean old JARs on EC2
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ubuntu
          key: ${{ secrets.EC2_SSH_KEY }}
          script: |
            echo "🧹 Removing old JAR files..."
            rm -f ~/calculator-CI-CD/target/*.jar || true

      - name: Copy JAR to EC2
        uses: appleboy/scp-action@v0.1.4
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ubuntu
          key: ${{ secrets.EC2_SSH_KEY }}
          source: "target/*.jar"
          target: "~/calculator-CI-CD/target"

      - name: Restart Spring Boot App on EC2
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ubuntu
          key: ${{ secrets.EC2_SSH_KEY }}
          command_timeout: 5m
          script: |
            echo "📁 Navigating to app directory..."
            cd ~/calculator-CI-CD/target

#            echo "🛑 Stopping existing Spring Boot app..."
#            pkill -f "java" || true
#
#            echo "🔍 Searching for JAR file..."
#            JAR_FILE=$(ls *.jar | grep -v '.original' | head -n 1)
#
#            if [ -z "$JAR_FILE" ]; then
#              echo "❌ No JAR file found to run!"
#              exit 1
#            fi
#
#            echo "🚀 Starting $JAR_FILE..."
#            nohup java -jar "$JAR_FILE" > ~/log.txt 2>&1 &
#
#            sleep 2
            echo "✅ Spring Boot app launched in background."
